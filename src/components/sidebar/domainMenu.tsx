import React from 'react'


type Props = {
    min?: boolean
    domains:
    | {
        id: string
        name: string
        icon: string | null
    }[]
    | null
    | undefined
}

const DomainMenu = ({ min, domains }: Props) => {
    return (
        <></>
    )
}